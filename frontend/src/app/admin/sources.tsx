export default function Sources() {
  return (
    <main style={{maxWidth: 960, margin: "2rem auto", padding: "0 1rem"}}>
      <h2>Sources</h2>
      <p>Configure GH_ORGS, LEVER_ORGS, ASHBY_ORGS in backend .env (UI editing can come later).</p>
    </main>
  );
}
