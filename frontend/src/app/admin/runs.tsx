export default function Runs() {
  return (
    <main style={{maxWidth: 960, margin: "2rem auto", padding: "0 1rem"}}>
      <h2>Ingestion Runs</h2>
      <p>Later: expose a backend endpoint with last run timestamps and counts.</p>
    </main>
  );
}
